Imagine you are a senior c# developer. In the provided @task-3.cs file the PaymentProcessingService intermittently throws a NullReferenceException when deserializing the gateway’s JSON response (which can be empty or malformed), and lacks any resilience logic (e.g. retries or circuit breaker), leading to unhandled errors at runtime. You should analyze that code and provide some potential reasons for it not not to work as intended. Also provide ways to make code more robust and resilient compared to the current one. Then choose the solution that most likely fixes the issue and provide a full fixed code
